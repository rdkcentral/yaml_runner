[project]
name = "yaml_runner"
version = "2.1.1"
dependencies = [
    "PyYaml>=6.0",
]
[tool.setuptools.packages.find]
include = ["yaml_runner", "yaml_runner.engines"]
where = ["src"]
namespaces = false

[project.scripts]
yaml_runner = "yaml_runner.cli:main"
